import{_ as a,o as e,c as s,d as o}from"./app.d9aec601.js";const n={},t=o(`<h1 id="using-binary-lua-libraries" tabindex="-1"><a class="header-anchor" href="#using-binary-lua-libraries" aria-hidden="true">#</a> Using Binary Lua Libraries</h1><p>On POSIX systems, JVM loads dynamical libraries with <code>dlopen</code>. By default, it passes the <code>RTLD_LOCAL</code> flag to <code>dlopen</code>, making the imported symbols only visible to JVM, avoiding polluting the global symbol space (like namespace pollution).</p><p>However, if you are loading a binary Lua package, it will use some of the Lua C APIs, which means it needs accessible to those imported symbols. To make those symbols visible to our external libraries, we will need to make them global by re-opening the library with a <code>RTLD_GLOBAL</code> flag.</p><div class="language-console ext-console"><pre class="language-console"><code>$ # Without the \`RTLD_GLOBAL\` flag
$ java -jar example/build/libs/example-all.jar --lua 5.1 -e &#39;require(&quot;lfs&quot;)&#39;
java.lang.RuntimeException: error loading module &#39;lfs&#39; from file &#39;./lfs.so&#39;:
	./lfs.so: undefined symbol: lua_gettop
$ # Re-opening with \`RTLD_GLOBAL\`
$ java -jar example/build/libs/example-all.jar --global --lua 5.1 -e &#39;require(&quot;lfs&quot;)&#39;
</code></pre></div><p>However, this comes at a risk. If you ever tries to load two versions of Lua, say Lua 5.1 and Lua 5.2, into JVM, with both of them global or one of them global, you will have no way to tell symbols from one version to those from another. JVM will very likely just crash on this.</p><p>We prevent loading multiple global Lua versions by some simple checks, so you will probably see some exceptions instead of a VM crash. But still, doing so is risky and will make your application hardly portable.</p><div class="custom-container warning"><p class="custom-container-title">WARNING</p><p>Also, loading libraries as global is not tested on many platforms, since I don&#39;t want to have to port the tests over every one of them.</p><p>You&#39;ve been warned. But issues are welcome if you encounter any problems.</p></div><p>To re-open the Lua library as global, you may do something like this:</p><div class="language-java ext-java"><pre class="language-java"><code><span class="token class-name">Lua</span> <span class="token class-name">L</span> <span class="token operator">=</span> <span class="token function">getLua</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token class-name">L</span><span class="token punctuation">.</span><span class="token function">getLuaNative</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">loadAsGlobal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>You only need to do this once per JVM, but it is safe to <code>loadAsGlobal</code> more than once.</p>`,10),l=[t];function i(p,r){return e(),s("div",null,l)}var u=a(n,[["render",i],["__file","rtld.html.vue"]]);export{u as default};
